export const simpleapprove = {
  "type": "object",
  "required": ["decision"],
  "properties": {
    "decision": { "type": "string", "enum": ["", "approve", "reject"] },
    "description": { "type": "string", "default":"" }
  }
}
